# Example env for scripts/gcloud-backend (do not commit secrets)
GCS_BUCKET=your-gcs-bucket
CORS_ORIGINS=http://localhost:3030,http://localhost:3000,https://your-store.com
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
# Optional later: protect list/admin endpoints
# STAFF_API_KEY=
